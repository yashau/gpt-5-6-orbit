interface:
  display_name: "GPT-5.6 Orbit"
  short_description: "Route work across Sol, Terra, and Luna"
  default_prompt: "Use $gpt-5-6-orbit to plan with Sol, build with Terra, and close out with Luna."

policy:
  allow_implicit_invocation: false
